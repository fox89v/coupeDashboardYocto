#include "graphicsplugin.h"
#include <QtQml/qqml.h>

void GraphicsPlugin::registerTypes(const char *uri)
{
    // placeholder: nessun tipo registrato (per ora)
    Q_ASSERT(uri && QByteArray(uri) == "Sa.Graphics");
}
